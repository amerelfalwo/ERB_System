# نظام إدارة المخازن والمبيعات الاحترافي (Advanced ERP Lite)

هذا المستند يوضح خطة العمل الكاملة لبناء نظام إدارة مخازن متكامل يعتمد على نظام الدفعات (Batches) لتعدد الأسعار، ونظام مدفوعات مرن يدعم الدفع الجزئي، مع محرك ديناميكي لطباعة الفواتير.

## التقنيات المستخدمة (Tech Stack)
* **اللغة:** Python 3.10+
* **إطار العمل:** FastAPI
* **قاعدة البيانات:** PostgreSQL
* **محرك التعامل مع الداتا بيز (ORM):** SQLAlchemy
* **إدارة الحزم والبيئة:** `uv`
* **نظام التحكم في الإصدارات:** Git (الفرع الأساسي: `main`)
* **نظام القوالب (للطباعة):** Jinja2

---

## المرحلة الأولى: إعداد بيئة العمل (Project Initialization)
* [ ] إنشاء مجلد المشروع وتهيئته باستخدام `uv init`.
* [ ] تهيئة مستودع Git وتغيير الفرع إلى `main`.
* [ ] إنشاء ملف `.env` لإدارة متغيرات البيئة (بيانات PostgreSQL).
* [ ] إعداد الهيكل التنظيمي للمجلدات (App, Core, Models, Schemas, Routers, Services).
* [ ] ضبط `uv` لتحميل المكتبات الأساسية: `fastapi`, `uvicorn`, `sqlalchemy`, `psycopg2-binary`, `pydantic`.

## المرحلة الثانية: تصميم قاعدة البيانات (Database Modeling)
تعتمد هذه المرحلة على بناء علاقات متينة تضمن دقة البيانات:
* **جدول المنتجات (Products):** الاسم والبيانات الأساسية فقط.
* **جدول الدفعات (Stock Batches):** * يربط المنتج بسعر شراء وسعر بيع محدد.
    * تتبع الكمية المتبقية في كل دفعة بشكل منفصل.
* **جدول الأطراف (Parties):** يجمع العملاء والموردين مع تمييز النوع.
* **جدول الفواتير (Invoices):** * يدعم نوعين (شراء/بيع).
    * يربط الفاتورة بالأطراف.
* **جدول تفاصيل الفواتير (Invoice Items):** يربط كل صنف بالدفعة (Batch) التي تم السحب منها أو الإضافة إليها.
* **جدول المدفوعات (Payments):** لتسجيل الدفعات النقدية (الكلية والجزئية) وربطها بالفواتير.
* **جدول القوالب (Print Templates):** لتخزين كود HTML وإعدادات الطباعة (JSON).

## المرحلة الثالثة: منطق العمل والـ Backend (Business Logic)
* **إدارة المخزن (Inventory Logic):**
    * عند الشراء: إنشاء دفعة (Batch) جديدة تلقائياً.
    * عند البيع: استخدام نظام FIFO (الأقدم أولاً) لخصم الكميات من الدفعات المتاحة.
* **الحسابات المالية:**
    * تطوير Logic لحساب الرصيد المتبقي على العميل/المورد (إجمالي الفواتير - إجمالي المدفوعات).
    * تتبع حالة الفاتورة (مدفوعة، مدفوعة جزئياً، غير مدفوعة).
* **نظام الـ Admin:**
    * Endpoints خاصة للتحكم الكامل في الدفعات وتعديل الأسعار يدوياً.

## المرحلة الرابعة: نظام طباعة الفواتير الديناميكي
* تطوير محرك داخلي يستخدم `Jinja2` لدمج بيانات الفاتورة مع قالب HTML.
* إتاحة التحكم للـ Admin عبر واجهة برمجة التطبيقات لتحديد العناصر المراد إظهارها (مثل إخفاء السعر القديم، إظهار الشعار، إلخ) عبر حقل `JSON settings`.
* توفير إمكانية معاينة الفاتورة قبل الطباعة.

## المرحلة الخامسة: لوحة التحكم والتقارير (Dashboard & Analytics)
* **تقرير الأرباح:** حساب الربح بناءً على الفرق بين سعر بيع الدفعة وسعر شرائها الفعلي.
* **تقرير المخزن:** عرض قائمة بالمنتجات مع تفصيل الدفعات (كم كمية بالسعر القديم وكم كمية بالسعر الجديد).
* **كشف حساب:** استخراج تقرير مفصل لكل عميل أو مورد يشمل كافة حركاته المالية.

## المرحلة السادسة: الاختبار والتدشين (Testing & Deployment)
* كتابة Unit Tests للعمليات الحساسة (خصم الكميات، حساب الأرباح).
* إجراء تجارب على سيناريوهات الدفع الجزئي وتداخل الدفعات.
* التأكد من أداء الاستعلامات (Queries) مع أحجام بيانات كبيرة في PostgreSQL.

---
**ملاحظة:** يتم كتابة الكود بالكامل بدون تعليقات (Comments) بناءً على تفضيلات المشروع المسبقة.
